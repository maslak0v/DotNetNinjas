services:
  financialtracker.services.authorizeapi:
    image: ${DOCKER_REGISTRY-}financialtrackerservicesauthorizeapi
    build:
      context: .
      dockerfile: FinancialTracker.Services.AuthorizeApi/Dockerfile
    env_file: ./SettingsData.env
    ports:
       - "5010:5010"
      # - "5001:5001"
    container_name: auth_webapi
    depends_on:
      - postgres_db
    networks:
      - my_network
    #command: ["dotnet", "ef", "database", "update", "--no-build", "--project", "FinancialTracker.Services.AuthorizeApi.csproj", "--startup-project", "."]

  postgres_db:
    image: postgres:latest
    container_name: db_postgesql_container
    restart: always
    ports:
      - "5432:5432"
    env_file: ./SettingsData.env
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - my_network
  pgadmin: 
    image: dpage/pgadmin4
    container_name: pgadmin4_container
    restart: always
    ports:
      - "8081:80"
    env_file: ./SettingsData.env
    volumes:
      - pgadmindata:/var/lib/pgadmin
    networks:
      - my_network

  financialtracker.services.analytics:
    image: ${DOCKER_REGISTRY-}financialtrackerservices
    build:
      context: .
      dockerfile: FinancialTracker.Services.Analytics/Dockerfile
    env_file: ./SettingsData.env
    ports:
      - "5030:5030"
    container_name: analytics_webapi
    depends_on:
      - postgres_db
      - financialtracker.services.authorizeapi
    networks:
      - my_network

volumes:
  pg_data:
  pgadmindata:

networks:
  my_network:
    driver: bridge
